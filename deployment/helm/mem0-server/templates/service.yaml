apiVersion: v1
kind: Service
metadata:
  name: {{ include "mem0-server.fullname" . }}
  labels:
    {{- include "mem0-server.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mem0-server.selectorLabels" . | nindent 4 }}
