{{- if .Values.mem0.extraConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mem0-server.fullname" . }}
  labels:
    {{- include "mem0-server.labels" . | nindent 4 }}
data:
  config.json: |
    {{- toPrettyJson .Values.mem0.extraConfig | nindent 4 }}
{{- end }}
